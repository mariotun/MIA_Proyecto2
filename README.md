## Proyecto #2
### Aplicacion web de compra y venta de productos usando angular,nodejs y oracle.
- Antes de iniciar el servidor del backend se debe declarar una variable global (export LD_LIBRARY_PATH=/opt/instantclient_19_8/), luego ya podemos correr el servidor con (node src/index.js)
